{
  "name": "api-with-tokenauth",
  "version": "1.0.0",
  "description": "A webservice that uses TokenAuth as authentication method and JWTs as authorization tokens.",
  "main": "src/index.js",
  "scripts": {
    "start": "clear && node src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gustavo Oliveira",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "jsonwebtoken": "^8.5.1",
    "morgan": "^1.10.0",
    "uuid": "^8.3.2"
  }
}
